<Window x:Class="RightMgr.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:services="clr-namespace:RightMgr.Services"
        Title="{services:Loc app_title}"
        Width="1360" Height="840" MinWidth="1100" MinHeight="700"
        Background="{DynamicResource WindowBg}"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        TextOptions.TextFormattingMode="Display"
        WindowStyle="None"
        ResizeMode="CanResize"
        DragOver="Window_DragOver"
        Drop="Window_Drop">
    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBg" Color="#F3F4F6" />
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SurfaceAlt" Color="#F9FAFB" />
        <SolidColorBrush x:Key="SurfaceRaised" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ControlBg" Color="#FFFFFF" />
        <SolidColorBrush x:Key="ControlHover" Color="#F3F4F6" />
        <SolidColorBrush x:Key="Ink" Color="#111827" />
        <SolidColorBrush x:Key="Muted" Color="#6B7280" />
        <SolidColorBrush x:Key="Subtle" Color="#4B5563" />
        <SolidColorBrush x:Key="Line" Color="#D1D5DB" />
        <SolidColorBrush x:Key="Panel" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Side" Color="#111827" />
        <SolidColorBrush x:Key="SideMuted" Color="#E5E7EB" />
        <SolidColorBrush x:Key="SideSubtle" Color="#A7B0C0" />
        <SolidColorBrush x:Key="SideSelection" Color="#243044" />
        <SolidColorBrush x:Key="SideHover" Color="#1B2433" />
        <SolidColorBrush x:Key="Accent" Color="#1D4ED8" />
        <SolidColorBrush x:Key="AccentText" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Danger" Color="#B91C1C" />

        <sys:Double x:Key="FontSizeLabel">12</sys:Double>
        <sys:Double x:Key="FontSizeBody">13</sys:Double>
        <sys:Double x:Key="FontSizeTitle">20</sys:Double>
        <sys:Double x:Key="FontSizeHero">28</sys:Double>
        <sys:Double x:Key="ControlHeight">32</sys:Double>
        <sys:Double x:Key="DetailTwoLineHeight">46</sys:Double>
        <Thickness x:Key="ControlPadding">8,5</Thickness>
        <Thickness x:Key="ButtonPadding">12,5</Thickness>
        <Thickness x:Key="FieldLabelMargin">0,12,0,4</Thickness>
        <Thickness x:Key="InlineIconMargin">0,0,6,0</Thickness>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="{StaticResource ControlHeight}" />
            <Setter Property="Padding" Value="{StaticResource ButtonPadding}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Line}" />
            <Setter Property="Background" Value="{DynamicResource ControlBg}" />
            <Setter Property="Foreground" Value="{DynamicResource Ink}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHover}" />
                                <Setter Property="Foreground" Value="{DynamicResource Ink}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceRaised}" />
                                <Setter Property="Foreground" Value="{DynamicResource Ink}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="18" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Muted}" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="9" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHover}" />
                                <Setter Property="Foreground" Value="{DynamicResource Ink}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceRaised}" />
                                <Setter Property="Foreground" Value="{DynamicResource Ink}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowDangerButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Setter Property="Foreground" Value="{DynamicResource Danger}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHover}" />
                                <Setter Property="Foreground" Value="{DynamicResource Danger}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceRaised}" />
                                <Setter Property="Foreground" Value="{DynamicResource Danger}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource Accent}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
            <Setter Property="Foreground" Value="{DynamicResource AccentText}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.88" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentText}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.76" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentText}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{DynamicResource Danger}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlHover}" />
                                <Setter Property="Foreground" Value="{DynamicResource Danger}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SurfaceRaised}" />
                                <Setter Property="Foreground" Value="{DynamicResource Danger}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SideIconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SideHover}" />
                                <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SideSelection}" />
                                <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BaseTextBox" TargetType="TextBox">
            <Setter Property="MinHeight" Value="{StaticResource ControlHeight}" />
            <Setter Property="Padding" Value="{StaticResource ControlPadding}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Line}" />
            <Setter Property="Background" Value="{DynamicResource ControlBg}" />
            <Setter Property="Foreground" Value="{DynamicResource Ink}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource BaseTextBox}" />

        <Style x:Key="SelectableTextBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Foreground" Value="{DynamicResource Ink}" />
        </Style>

        <Style x:Key="SideTitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource FontSizeLabel}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Subtle}" />
            <Setter Property="Margin" Value="{StaticResource FieldLabelMargin}" />
        </Style>

        <Style x:Key="DetailTextBox" TargetType="TextBox" BasedOn="{StaticResource BaseTextBox}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAlt}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Line}" />
            <Setter Property="Foreground" Value="{DynamicResource Ink}" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
        </Style>

        <Style x:Key="ReadOnlyDetailTextBox" TargetType="TextBox" BasedOn="{StaticResource DetailTextBox}">
            <Setter Property="IsReadOnly" Value="True" />
        </Style>

        <Style x:Key="WrappingReadOnlyDetailTextBox" TargetType="TextBox" BasedOn="{StaticResource ReadOnlyDetailTextBox}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Height" Value="{StaticResource DetailTwoLineHeight}" />
        </Style>

        <Style x:Key="MultilineDetailTextBox" TargetType="TextBox" BasedOn="{StaticResource DetailTextBox}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="Height" Value="{StaticResource DetailTwoLineHeight}" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Ink}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Box" Width="16" Height="16" CornerRadius="3" BorderThickness="1"
                                    BorderBrush="{DynamicResource Line}" Background="{DynamicResource ControlBg}" VerticalAlignment="Center">
                                <TextBlock x:Name="CheckMark" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                                           Foreground="{DynamicResource AccentText}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="7,0,0,0" RecognizesAccessKey="True"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{DynamicResource Accent}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
        </Style>

        <Style x:Key="CategoryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Row" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{DynamicResource SideHover}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Row" Property="Background" Value="{DynamicResource SideSelection}" />
                                <Setter Property="Foreground" Value="{DynamicResource SideMuted}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EntryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="2" Background="{DynamicResource Side}" Padding="18,24,18,18" MouseLeftButtonDown="DragArea_MouseLeftButtonDown">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <DockPanel MouseLeftButtonDown="DragArea_MouseLeftButtonDown">
                        <Button x:Name="ThemeButton" DockPanel.Dock="Right" Width="30" Height="30"
                                Style="{StaticResource SideIconButton}" Click="ThemeButton_Click">
                            <TextBlock x:Name="ThemeButtonIcon" Text="&#xE771;" FontFamily="Segoe MDL2 Assets" FontSize="15" />
                        </Button>
                        <TextBlock Text="{services:Loc app_title}" FontSize="{StaticResource FontSizeHero}" FontWeight="SemiBold" Foreground="{DynamicResource SideMuted}" />
                    </DockPanel>
                    <TextBox x:Name="HeaderSummaryText" Text="{services:Loc app_subtitle_scanning}" Foreground="{DynamicResource SideSubtle}" Style="{StaticResource SelectableTextBox}" Margin="0,6,0,18" />

                    <TextBlock Text="{services:Loc search_placeholder}" Foreground="{DynamicResource SideSubtle}" FontSize="{StaticResource FontSizeLabel}" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" />
                        <Button x:Name="FilterButton" Grid.Column="1" Click="FilterButton_Click" Margin="8,0,0,0" Padding="{StaticResource ControlPadding}" ToolTip="{services:Loc search_filter}">
                            <TextBlock Text="&#xE71C;" FontFamily="Segoe MDL2 Assets" />
                        </Button>
                        <Popup x:Name="FilterPopup" Grid.ColumnSpan="2" PlacementTarget="{Binding ElementName=FilterButton}" Placement="Bottom" StaysOpen="False">
                            <Border Background="{DynamicResource SurfaceRaised}" BorderBrush="{DynamicResource Line}" BorderThickness="1" Padding="14" Width="250">
                                <StackPanel>
                                    <TextBlock Text="{services:Loc search_filter}" FontWeight="SemiBold" Margin="0,0,0,10" />
                                    <CheckBox x:Name="UseRegexBox" Content="{services:Loc regex_enabled}" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" Margin="0,0,0,10" />
                                    <CheckBox x:Name="SearchNameBox" Content="{services:Loc search_name}" IsChecked="True" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" Margin="0,0,0,5" />
                                    <CheckBox x:Name="SearchCategoryBox" Content="{services:Loc search_category}" IsChecked="True" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" Margin="0,0,0,5" />
                                    <CheckBox x:Name="SearchRegistryBox" Content="{services:Loc search_registry}" IsChecked="True" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" Margin="0,0,0,5" />
                                    <CheckBox x:Name="SearchValueBox" Content="{services:Loc search_value}" IsChecked="True" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" Margin="0,0,0,5" />
                                    <CheckBox x:Name="SearchComBox" Content="{services:Loc search_com}" IsChecked="True" Checked="SearchOptions_Changed" Unchecked="SearchOptions_Changed" />
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,14,0,16">
                        <Button Click="RefreshButton_Click" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Margin="{StaticResource InlineIconMargin}" />
                                <TextBlock Text="{services:Loc action_refresh}" />
                            </StackPanel>
                        </Button>
                        <Button Click="ExportButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xEDE1;" FontFamily="Segoe MDL2 Assets" Margin="{StaticResource InlineIconMargin}" />
                                <TextBlock Text="{services:Loc action_export}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <TextBlock Text="{services:Loc nav_title}" Style="{StaticResource SideTitleText}" Margin="0,0,0,8" />
                </StackPanel>

                <ListBox x:Name="CategoryList" SelectionChanged="CategoryList_SelectionChanged" ItemContainerStyle="{StaticResource CategoryItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="{DynamicResource SideMuted}" TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource Surface}" MouseLeftButtonDown="DragArea_MouseLeftButtonDown">
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource WindowButton}" Content="&#xE921;" Click="MinimizeButton_Click" ToolTip="Minimize" />
                        <Button x:Name="MaximizeButton" Style="{StaticResource WindowButton}" Content="&#xE922;" Click="MaximizeButton_Click" ToolTip="Maximize" />
                        <Button Style="{StaticResource WindowDangerButton}" Content="&#xE8BB;" Click="CloseButton_Click" ToolTip="Close" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Padding="18,0,18,10" MouseLeftButtonDown="DragArea_MouseLeftButtonDown">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox x:Name="EnableSwitch" Content="{services:Loc enable}" Checked="EnableSwitch_Changed" Unchecked="EnableSwitch_Changed" Margin="0,0,14,0" VerticalAlignment="Center" VerticalContentAlignment="Center" />
                        <Button x:Name="SaveButton" Content="{services:Loc action_save}" Click="SaveButton_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" />
                        <Button x:Name="DeleteButton" Content="{services:Loc action_delete}" Click="DeleteButton_Click" Style="{StaticResource DangerButton}" Margin="0,0,8,0" />
                        <Button Content="{services:Loc action_copy}" Click="CopyAllButton_Click" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="ListTitleText" Text="{services:Loc list_title}" FontSize="{StaticResource FontSizeTitle}" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                        <TextBlock x:Name="CountText" Foreground="{DynamicResource Muted}" FontSize="{StaticResource FontSizeBody}" Margin="10,1,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="ListPaneColumn" Width="420" MinWidth="258" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition x:Name="DetailPaneColumn" Width="*" MinWidth="360" />
                </Grid.ColumnDefinitions>

                <Border Background="{DynamicResource SurfaceAlt}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Padding="14,10" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Background="{DynamicResource WindowBg}">
                            <Grid x:Name="CurrentListToolbar" SizeChanged="CurrentListToolbar_SizeChanged">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" MinWidth="0" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <CheckBox x:Name="ShellVerbFilterBox" IsChecked="True" Checked="TypeFilter_Changed" Unchecked="TypeFilter_Changed" Margin="0,0,18,0" VerticalAlignment="Center" VerticalContentAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" Margin="{StaticResource InlineIconMargin}" VerticalAlignment="Center" />
                                            <TextBlock Text="{services:Loc filter_shell}" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </CheckBox>
                                    <CheckBox x:Name="ShellExFilterBox" IsChecked="True" Checked="TypeFilter_Changed" Unchecked="TypeFilter_Changed" VerticalAlignment="Center" VerticalContentAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource Accent}" Margin="{StaticResource InlineIconMargin}" VerticalAlignment="Center" />
                                            <TextBlock Text="{services:Loc filter_shellex}" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </CheckBox>
                                </StackPanel>

                                <Grid x:Name="CurrentListSearchPanel" Grid.Column="1" Width="230" Margin="14,0,0,0" VerticalAlignment="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="CurrentListSearchBox" TextChanged="CurrentListSearchBox_TextChanged" ToolTip="{services:Loc list_search_placeholder}" />
                                    <Button x:Name="CurrentListFilterButton" Grid.Column="1" Click="CurrentListFilterButton_Click" Margin="8,0,0,0" Padding="{StaticResource ControlPadding}" ToolTip="{services:Loc list_search_filter}">
                                        <TextBlock Text="&#xE71C;" FontFamily="Segoe MDL2 Assets" />
                                    </Button>
                                    <Popup x:Name="CurrentListFilterPopup" Grid.ColumnSpan="2" PlacementTarget="{Binding ElementName=CurrentListFilterButton}" Placement="Bottom" StaysOpen="False">
                                        <Border Background="{DynamicResource SurfaceRaised}" BorderBrush="{DynamicResource Line}" BorderThickness="1" Padding="14" Width="250">
                                            <StackPanel>
                                                <TextBlock Text="{services:Loc list_search_filter}" FontWeight="SemiBold" Margin="0,0,0,10" />
                                                <CheckBox x:Name="CurrentUseRegexBox" Content="{services:Loc regex_enabled}" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" Margin="0,0,0,10" />
                                                <CheckBox x:Name="CurrentSearchNameBox" Content="{services:Loc search_name}" IsChecked="True" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" Margin="0,0,0,5" />
                                                <CheckBox x:Name="CurrentSearchCategoryBox" Content="{services:Loc search_category}" IsChecked="True" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" Margin="0,0,0,5" />
                                                <CheckBox x:Name="CurrentSearchRegistryBox" Content="{services:Loc search_registry}" IsChecked="True" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" Margin="0,0,0,5" />
                                                <CheckBox x:Name="CurrentSearchValueBox" Content="{services:Loc search_value}" IsChecked="True" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" Margin="0,0,0,5" />
                                                <CheckBox x:Name="CurrentSearchComBox" Content="{services:Loc search_com}" IsChecked="True" Checked="CurrentSearchOptions_Changed" Unchecked="CurrentSearchOptions_Changed" />
                                            </StackPanel>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Grid>
                        </Border>

                        <ListBox x:Name="ItemsList" Grid.Row="1" SelectionChanged="ItemsList_SelectionChanged" ItemContainerStyle="{StaticResource EntryItemStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="14,11" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Background="{DynamicResource Surface}">
                                        <DockPanel>
                                            <Image DockPanel.Dock="Left" Width="22" Height="22" Source="{Binding IconImageSource}" Margin="0,2,11,0" />
                                            <StackPanel>
                                                <TextBlock Text="{Binding CompactTitle}" FontWeight="SemiBold" Foreground="{DynamicResource Ink}" TextTrimming="CharacterEllipsis" TextDecorations="{Binding PendingDeleteTextDecorations}" />
                                                <TextBlock Text="{Binding CompactSubtitle}" FontSize="{StaticResource FontSizeLabel}" Foreground="{DynamicResource Muted}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <GridSplitter Grid.Column="1"
                              Width="5"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Background="{DynamicResource Line}"
                              ShowsPreview="False"
                              DragCompleted="ContentSplitter_DragCompleted" />

                <Grid Grid.Column="2" Background="{DynamicResource Surface}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Padding="18" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1">
                        <DockPanel LastChildFill="True">
                            <Image x:Name="HeaderIcon" DockPanel.Dock="Left" Width="44" Height="44" Margin="0,0,14,0" Visibility="Collapsed" />
                            <StackPanel>
                                <TextBox x:Name="MenuNameText" Text="{services:Loc detail_empty_title}" FontSize="{StaticResource FontSizeTitle}" FontWeight="SemiBold" TextWrapping="Wrap" Style="{StaticResource SelectableTextBox}" />
                                <TextBox x:Name="DescriptionText" Foreground="{DynamicResource Muted}" TextWrapping="Wrap" Style="{StaticResource SelectableTextBox}" Margin="0,5,0,0" />
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="18,6,18,18">
                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="{services:Loc field_category}" Style="{StaticResource FieldLabel}" />
                                    <TextBox x:Name="CategoryBox" Style="{StaticResource ReadOnlyDetailTextBox}" />
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="{services:Loc field_scope}" Style="{StaticResource FieldLabel}" />
                                    <TextBox x:Name="ScopeBox" Style="{StaticResource ReadOnlyDetailTextBox}" />
                                </StackPanel>
                            </UniformGrid>

                            <DockPanel Margin="0,12,0,4">
                                <Button DockPanel.Dock="Right" Click="OpenRegistryButton_Click" Margin="10,0,0,0" Content="{services:Loc action_open_registry}" />
                                <TextBlock Text="{services:Loc field_registry_path}" Style="{StaticResource FieldLabel}" Margin="0" VerticalAlignment="Center" />
                            </DockPanel>
                            <TextBox x:Name="RegistryPathBox" Style="{StaticResource WrappingReadOnlyDetailTextBox}" />

                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="{services:Loc field_key}" Style="{StaticResource FieldLabel}" />
                                    <TextBox x:Name="KeyNameBox" Style="{StaticResource ReadOnlyDetailTextBox}" />
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="{services:Loc field_value_name}" Style="{StaticResource FieldLabel}" />
                                    <TextBox x:Name="ValueNameBox" Style="{StaticResource ReadOnlyDetailTextBox}" />
                                </StackPanel>
                            </UniformGrid>

                            <TextBlock Text="{services:Loc field_value}" Style="{StaticResource FieldLabel}" />
                            <TextBox x:Name="ValueBox" Style="{StaticResource MultilineDetailTextBox}" />

                            <DockPanel Margin="0,12,0,4">
                                <Button x:Name="OpenClsidButton" DockPanel.Dock="Right" Click="OpenClsidRegistryButton_Click" Margin="10,0,0,0" Content="{services:Loc action_open_clsid}" />
                                <TextBlock Text="{services:Loc field_clsid}" Style="{StaticResource FieldLabel}" Margin="0" VerticalAlignment="Center" />
                            </DockPanel>
                            <TextBox x:Name="ClsidBox" Style="{StaticResource ReadOnlyDetailTextBox}" />

                            <DockPanel Margin="0,12,0,4">
                                <Button x:Name="OpenDllPathButton" DockPanel.Dock="Right" Click="OpenDllPathButton_Click" Margin="10,0,0,0" Content="{services:Loc action_open_path}" />
                                <TextBlock Text="{services:Loc field_dll}" Style="{StaticResource FieldLabel}" Margin="0" VerticalAlignment="Center" />
                            </DockPanel>
                            <TextBox x:Name="DllPathBox" Style="{StaticResource WrappingReadOnlyDetailTextBox}" />

                            <DockPanel Margin="0,12,0,4">
                                <Button x:Name="OpenIconPathButton" DockPanel.Dock="Right" Click="OpenIconPathButton_Click" Margin="10,0,0,0" Content="{services:Loc action_open_path}" />
                                <Button DockPanel.Dock="Right" Click="ChooseIconButton_Click" Margin="10,0,0,0" Content="{services:Loc action_choose_icon}" />
                                <TextBlock Text="{services:Loc field_icon}" Style="{StaticResource FieldLabel}" Margin="0" VerticalAlignment="Center" />
                            </DockPanel>
                            <TextBox x:Name="IconResourceBox" Style="{StaticResource MultilineDetailTextBox}" />
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>

        <Border Grid.Column="1" Grid.Row="1" Padding="14,8" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox Text="{services:Loc status_export_hint}" Foreground="{DynamicResource Muted}" Style="{StaticResource SelectableTextBox}" />
                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center">
                        <Hyperlink NavigateUri="https://github.com/ohyooo/RightMgr" RequestNavigate="SourceCodeLink_RequestNavigate" Foreground="{DynamicResource Muted}">
                            源代码
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
                <TextBox x:Name="StatusText" Style="{StaticResource SelectableTextBox}" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
